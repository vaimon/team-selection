-- У заявок не было отметки времени вовсе, поэтому вопрос «сколько заявок висит дольше N дней»
-- администратору ответить было нечем (#10). Существующим строкам достаётся now(): их возраст
-- бессмысленен, но перед открытием набора база сбрасывается, и живых старых строк там не будет.
ALTER TABLE applications
    ADD COLUMN created_at timestamp without time zone NOT NULL DEFAULT now();
